{
  "name": "personal-finance-tracker-fullstack",
  "version": "1.0.0",
  "description": "DevOps & Fullstack - End-to-End Testing CI Pipeline",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd .. && npm install",
    "start-backend": "cd backend && npm start",
    "start-frontend": "cd frontend && npm start",
    "test-backend": "cd backend && npm test",
    "cypress-open": "cypress open",
    "cypress-run": "cypress run --headless"
  },
  "dependencies": {
    "cypress": "^13.6.2"
  },
  "keywords": ["devops", "fullstack", "ci-cd", "e2e-testing"],
  "author": "DevOps Team",
  "license": "MIT"
}
